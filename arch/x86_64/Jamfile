SubDir TOP kernel arch x86_64 ;

local asms = [ Glob $(SEARCH_SOURCE) : *.asm ] ;
local objs = $(asms:S=$(SUFOBJ):R=build) ;

Echo $(objs) ;

local flags = -felf64 ; 

Nasm kernel : $(asms) : $(flags) ;